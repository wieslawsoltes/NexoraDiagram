{
  "name": "nexora-diagram",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free structured diagram editor with a retained WebGPU renderer",
  "scripts": {
    "start": "node serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node build.mjs",
    "build:pages": "node build-pages.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
